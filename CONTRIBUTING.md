# 기여 안내

1. 기본 브랜치에 직접 push하지 않는다. 브랜치를 만들고 PR을 연다.
2. PR은 승인 1개와 리뷰 스레드 해결이 있어야 머지된다. 머지는 squash 또는 merge commit.
3. PR 본문은 템플릿대로 — 무엇을 왜 바꿨고, 어떻게 검증했는지.
4. 비밀값(.env, 토큰, 키)은 커밋하지 않는다. 푸시 보호가 막더라도 우회하지 말고 시크릿을 회전한다.
5. 의존성 취약점 알림(Dependabot)은 무시하지 않는다 — 고치거나, 왜 해당 없는지 닫으며 적는다.
